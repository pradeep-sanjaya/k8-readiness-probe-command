apiVersion: v1
kind: Pod
metadata:
  name: k8-readyness-probe
spec:
  containers:
  - name: k8-readyness-probe
    image: k8-readyness-probe:1.0
    livenessProbe:
      exec:
        command:
        - cat
        - /tmp/health
      initialDelaySeconds: 5
      periodSeconds: 5
